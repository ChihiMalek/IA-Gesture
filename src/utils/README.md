# Utils

Reserved for shared helper functions (e.g. landmark preprocessing, MQTT
config constants) that could be factored out of
`src/gesture_recognition/handgestures.py` in a future refactor. No such
module existed in the original project, so nothing was moved here to avoid
altering behavior.
