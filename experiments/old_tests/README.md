# Old Tests

Reserved for deprecated or superseded Python test scripts, kept for
reference rather than deleted outright. This folder is currently empty —
the exploratory work that predates the final pipeline lives in
[`../prototypes/`](../prototypes/) instead.

For deprecated **firmware** test sketches (OLED test, MQTT test, blink
test) that preceded the final
[`firmware/gesture_assistant/gesture_assistant.ino`](../../firmware/gesture_assistant/gesture_assistant.ino),
see [`../../firmware/tests/`](../../firmware/tests/).
